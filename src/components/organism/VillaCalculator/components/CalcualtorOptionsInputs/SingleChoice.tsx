/* eslint-disable */
import React from 'react';

type TOption = {
  title: string;
  imgSrc: string;
  selected: boolean;
  onChange: (val: boolean) => void;
};

type TProps = {
  title: string;
  subTitle: string;
  options: string[];
};

export const SingleChoice = ({}: TProps) => {
  return;
};
/* eslint-enable */
